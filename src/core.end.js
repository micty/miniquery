
})(
    typeof global == 'object' ? global : this, // 在 Node 中，全局对象是 global；其他环境是 this
    Array,
    Boolean,
    Date,
    Error,
    Function,
    Math,
    Number,
    Object,
    RegExp,
    String
    /*, undefined */
);

